# Tamia4Life — Startup Phase 2 design deliverables

Uploaded from the local `TA Startup` pack and linked on the AZM Startup board (`/startup/tamia`).

| Asset | Board task |
| --- | --- |
| `brand-guidelines/` + Design System + Bundle | Brand guidelines & design system pack (`S TA 2.B`) |
| Prototype v0 + Phase 1 screens | Interactive prototypes (`S TA 2.P`) |
| Matching Engine v1 Spec | Matching engine v1 specification (`S TA 2.M`) |
| Portals Build Plan | Digital portals & assets build plan (`S TA 2.D`) |
